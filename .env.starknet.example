# Starknet RPC
STARKNET_RPC_URL_MAINNET=https://starknet-mainnet.public.blastapi.io/rpc/v0_8
STARKNET_RPC_URL_SEPOLIA=https://starknet-sepolia.public.blastapi.io/rpc/v0_8

# BTC quote provider (optional). If unset, starknet_getBtcRouteQuote returns deterministic fallback quote.
STARKNET_BTC_QUOTE_API_URL=
STARKNET_BTC_QUOTE_API_KEY=

# Execute bridge for starknet_executeIntentGuarded
# Selection order:
# - network=sepolia -> STARKNET_NATIVE_EXECUTE_COMMAND_SEPOLIA (if set)
# - network=mainnet -> STARKNET_NATIVE_EXECUTE_COMMAND_MAINNET (if set)
# - fallback -> STARKNET_EXECUTE_COMMAND
# Placeholders: {intent} {network} {amountUsd} {runId} {actionType} {routeId} {amount} {minAmountOut} {maxFeeBps}
STARKNET_NATIVE_EXECUTE_COMMAND_SEPOLIA=bash scripts/starknet-execute-example.sh "{intent}" "sepolia" "{amountUsd}" "{runId}" "{actionType}" "{routeId}" "{amount}" "{minAmountOut}" "{maxFeeBps}"
STARKNET_NATIVE_EXECUTE_COMMAND_MAINNET=bash scripts/starknet-execute-example.sh "{intent}" "mainnet" "{amountUsd}" "{runId}" "{actionType}" "{routeId}" "{amount}" "{minAmountOut}" "{maxFeeBps}"
STARKNET_EXECUTE_COMMAND=bash scripts/starknet-execute-example.sh "{intent}" "{network}" "{amountUsd}" "{runId}" "{actionType}" "{routeId}" "{amount}" "{minAmountOut}" "{maxFeeBps}"
