# NEAR runtime env template (OpenClaw / pi-chain-tools)
# Copy to your local secret store (do NOT commit real keys).

# --- Required for read-only wallet-aware tools ---
# Wallet/account id used by near_getBalance / near_getPortfolio / Burrow reads, etc.
NEAR_ACCOUNT_ID=your-account.near

# Optional: pick network rpc explicitly (default uses network mapping in runtime)
# Mainnet
NEAR_RPC_URL=https://rpc.mainnet.near.org
# Testnet (uncomment if needed)
# NEAR_RPC_URL=https://rpc.testnet.near.org

# --- Required for execute/compose-sign flows (real transactions) ---
# Either provide private key directly...
# NEAR_PRIVATE_KEY=ed25519:xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ...or point to NEAR credentials directory (recommended with near-cli login)
# NEAR_CREDENTIALS_DIR=~/.near-credentials

# Optional explicit wallet account id alias (if different env source is desired)
# NEAR_WALLET_ACCOUNT_ID=your-account.near

# --- Optional protocol/service overrides ---
# Ref/Burrow contract ids (mainnet defaults exist; only override if needed)
# NEAR_REF_CONTRACT_ID=v2.ref-finance.near
# NEAR_BURROW_CONTRACT_ID=contract.main.burrow.near

# NEAR Intents endpoints/auth (only needed for intents tools)
# NEAR_INTENTS_API_BASE_URL=https://1click.chaindefuser.com
# NEAR_INTENTS_JWT=...
# NEAR_INTENTS_API_KEY=...

# Intents explorer auth (only needed for explorer read tools)
# NEAR_INTENTS_EXPLORER_JWT=...

# --- Safety / behavior ---
# Default swap slippage bps for Ref swap helpers
# NEAR_SWAP_SLIPPAGE_BPS=100

# Yield worker webhook (optional, for near_yieldWorkerStart notifications)
# NEAR_YIELD_WORKER_WEBHOOK_URL=https://your-webhook-endpoint
