{
  "name": "@gradience/dashboard",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Dashboard runtime + API",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  }
}
