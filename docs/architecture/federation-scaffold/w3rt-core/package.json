{
  "name": "@gradience/w3rt-core",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Shared interfaces/types/policy contracts extracted from pi-chain-tools",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  }
}
