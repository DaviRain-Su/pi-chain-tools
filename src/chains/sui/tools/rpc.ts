import type { RegisteredTool } from "../../../core/types.js";

// Sui RPC-tool skeleton. Add raw JSON-RPC passthrough tools here if needed.
export function createSuiRpcTools(): RegisteredTool[] {
	return [];
}
