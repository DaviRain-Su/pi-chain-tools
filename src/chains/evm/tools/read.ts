import type { RegisteredTool } from "../../../core/types.js";

// EVM read-tool skeleton. Add non-mutating tools (balance, tx, block, logs) here.
export function createEvmReadTools(): RegisteredTool[] {
	return [];
}
